// import { ResponseHandler } from '../helpers';
// import db from '../models';

// class Feed {
//   static async getFeeds(req, res) {
//     const data = await db.findFeeds(req, res);
//     return ResponseHandler.success(res, 200, 'feeds retrieved', data);
//   }
// }

// export default Feed;
